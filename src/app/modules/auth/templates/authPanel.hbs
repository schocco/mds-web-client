<div class="right auth-panel">
    <span><i class="fa fa-user"></i>
    {{#if authenticated}}
        Logged in as <span data-region="current-user"></span> <br/>
        <a href="#logout" id="logout"><i class="fa fa-power-off"></i> log out</a>
    {{ else }}
        <a href="#login">log in</a>
    {{/if}}
    </span>
</div>